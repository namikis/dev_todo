name: Run Task (Claude)

on:
  workflow_dispatch:
    inputs:
      task_id:
        description: "Supabase task ID"
        required: true
      title:
        description: "Task title"
        required: true
      memo:
        description: "Task memo / instructions"
        required: false
        default: ""
      type:
        description: "Task type (research / implement)"
        required: false
        default: "implement"

permissions:
  contents: write
  pull-requests: write
  issues: write
  id-token: write

jobs:
  run-task:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
      TASK_ID: ${{ inputs.task_id }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set status to running
        run: node .github/scripts/update-status.mjs "$TASK_ID" running

      - name: Build Claude prompt
        id: prompt
        run: |
          MEMO="${{ inputs.memo }}"
          TITLE="${{ inputs.title }}"
          TYPE="${{ inputs.type }}"

          if [ "$TYPE" = "research" ]; then
            PROMPT="以下のタスクについて調査し、詳細なレポートをMarkdown形式で出力してください。"
          elif [ "$TYPE" = "implement" ]; then
            PROMPT="以下のタスクを実装してください。ブランチを切り、変更をコミットして、PRを作成するところまで行ってください。"
          else
            PROMPT="以下のタスクを実行してください。"
          fi

          PROMPT="${PROMPT}

          # ${TITLE}"

          if [ -n "$MEMO" ]; then
            PROMPT="${PROMPT}

          ${MEMO}"
          fi

          echo "prompt<<PROMPT_EOF" >> "$GITHUB_OUTPUT"
          echo "$PROMPT" >> "$GITHUB_OUTPUT"
          echo "PROMPT_EOF" >> "$GITHUB_OUTPUT"

      - name: Run Claude
        id: claude
        uses: anthropics/claude-code-action@v1
        with:
          prompt: ${{ steps.prompt.outputs.prompt }}
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          claude_args: "--allowedTools Bash Read Write Edit Glob Grep WebSearch WebFetch"

      - name: Extract PR URL
        id: pr
        if: always()
        run: |
          # Claude が作成した直近のPRを検索（最新1件）
          PR_URL=$(gh pr list --author "github-actions[bot]" --state open --json url --jq '.[0].url' 2>/dev/null || true)
          # bot 名が異なる場合のフォールバック: 直近10分以内のPRを探す
          if [ -z "$PR_URL" ]; then
            PR_URL=$(gh pr list --state open --limit 5 --json url,createdAt --jq 'sort_by(.createdAt) | reverse | .[0].url' 2>/dev/null || true)
          fi
          echo "pr_url=$PR_URL" >> "$GITHUB_OUTPUT"
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Finalize task (success)
        if: success()
        run: |
          ARGS=("$TASK_ID" "done")
          PR_URL="${{ steps.pr.outputs.pr_url }}"
          if [ -n "$PR_URL" ]; then
            ARGS+=("--pr-url=$PR_URL")
          fi
          ARGS+=("--result=Completed by GitHub Actions")
          node .github/scripts/finalize-task.mjs "${ARGS[@]}"

      - name: Finalize task (failure)
        if: failure()
        run: |
          node .github/scripts/finalize-task.mjs "$TASK_ID" error --result="GitHub Actions workflow failed"
